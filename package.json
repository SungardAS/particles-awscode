{
  "name": "particles-awscode",
  "version": "0.0.0",
  "description": "Condensation particles for building AWS CloudFormation templates.",
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "build": "./node_modules/.bin/gulp condensation:build",
    "deploy": "./node_modules/.bin/gulp condensation:deploy",
    "gulp": "./node_modules/.bin/gulp"
  },
  "license": "Apache-2.0",
  "keywords": [
    "condensation-particles",
    "aws",
    "cloudformation",
    "cloud",
    "formation"
  ],
  "dependencies": {
    "condensation": "^0.6.8",
    "config": "^1.26.1",
    "git-rev-sync": "^1.9.1",
    "gulp": "^3.9.1",
    "particles-core": "^0.5.3"
  },
  "devDependencies": {
    "condensation-particle-tests": "^0.5.4",
    "mocha": "^2.5.3"
  }
}
